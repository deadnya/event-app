export interface CompanyShortDTO {
    id: string;
    name: string;
}

export interface CreateCompanyDTO {
    name: string;
}

export interface EditCompanyDTO {
    id: string;
    name: string;
}
